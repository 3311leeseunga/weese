import React, { Component } from "react";

export default class join extends Component {
    render() {
        return (
            <div className="join">
                <div>JOIN</div>
            </div>
        );
    }
}
